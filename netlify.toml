# This tells Netlify how to build your Vite app
[build]
  command = "npm run build"
  publish = "dist"

# This is an alias for the production context
[context.production]
  command = "npm run build"
  publish = "dist"
